{% extends '::layout.admin.html.twig' %}

{% block content %}
    <h1>{{ 'admin.users.new.title' | trans }}</h1>
    <p>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'form.button.save' | trans }}" class="btn btn-primary" />
        {{ form_end(form) }}
    </p>
{% endblock %}