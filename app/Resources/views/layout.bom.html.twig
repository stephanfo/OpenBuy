{% extends "::master.html.twig" %}

{% block title %}
    {{ 'title.bom' | trans }} - {{ parent() }}
{% endblock %}

{% block container %}
    <div class="row">
        <div class="col-xl-2 col-md-3 col-sm-4">
            <h5>{{ 'leftmenu.title.quick_links' | trans }}</h5>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('bom_new') }}">{{ 'leftmenu.create_bom' | trans }}</a>
                </li>
            </ul>
            <h5 class="app-nav-sidebar-header">{{ 'leftmenu.title.boms' | trans }}</h5>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('bom_index') }}">{{ 'leftmenu.boms' | trans }}</a>
                </li>
            </ul>
        </div>
        <div class="col-xl-10 col-md-9 col-sm-8">
            {% block content %}{% endblock %}
        </div>
    </div>
{% endblock %}