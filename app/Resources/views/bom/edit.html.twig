{% extends '::layout.bom.html.twig' %}

{% block content %}
    <h1>{{ 'bom.edit.title' | trans }}</h1>
    <p>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'form.button.save' | trans }}" class="btn btn-primary pull-right" />
        {{ form_end(form) }}
    </p>
{% endblock %}